name: muunu.xd
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  shared_preferences: ^2.2.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/muunu.jpg"
  adaptive_icon_foreground: "assets/icon/muunu.jpg"
  adaptive_icon_background: "#FFFFFF"

flutter:
  uses-material-design: true